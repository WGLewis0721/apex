name: Phase 5 follow-up validation

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm ci
      - run: npm test
      - run: npm run build
        env:
          VITE_SUPABASE_URL: https://fnmxlmjrkgojowpzrcwa.supabase.co
          VITE_SUPABASE_PUBLISHABLE_KEY: sb_publishable_RmzipVOORKfFPlpdi3zheg_EQQth-xl
      - uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x
      - name: Check Stripe OAuth Edge Functions
        working-directory: supabase/functions
        run: deno check apex-stripe-connect/index.ts apex-stripe-connect-callback/index.ts
